sudo: false
language: python

os:
 - linux

python:
  - "3.6"

global:
  env:
    - COMMIT_AUTHOR_EMAIL: "f.zapata@esciencecenter.nl"

install:
  # install library
  - pip install .[test]

branches:
  only:
    - master
